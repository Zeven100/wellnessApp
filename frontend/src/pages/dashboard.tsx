import { Appbar } from "../components/appbar";

export const DashBoard = ()=>{
    return (
        <div>
           <Appbar/>
            // Add functionality to show all the blogs that this person 
            // has published and the functionality to delete the blogs .
        </div>
    )
}